services:
  iter8-bot:
    container_name: iter8-bot
    build: .
    env_file: ".secrets"
    ports:
      - "8080:8080"
    #    read_only: true

    #    volumes:
    #      - type: bind
    #        source: ./cogs
    #        target: /app/cogs
    #         Use the 'bind' key to pass the kernel mount options
    #        bind:
    #          options:
    #            - rw
    #            - noexec # Prevents execution of files in this directory

    restart: unless-stopped
